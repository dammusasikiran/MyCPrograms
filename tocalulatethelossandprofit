
#include <stdio.h>
int main()
{
    int cp,sp,l,p;
    printf("enter values\t");
    scanf("%d %d",&cp,&sp);
    if(sp>cp)
    {
        p=sp-cp;
        printf("%d",p);
    }
    else
    {
        l=cp-sp;
        printf("%d",l);
    }
    return 0;
}
